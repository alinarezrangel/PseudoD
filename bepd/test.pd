utilizar algor.pseudo
utilizar clases.pseudo
utilizar entsal.pseudo
utilizar mate.pseudo

clase Persona
	;padre
	;madre
	nombre
finclase
heredar Objeto Persona

instancia Persona A
instancia Persona B
instancia Persona C

redireccionar A#padre B#NOMBRE
redireccionar A#madre C#NOMBRE

fijar A#nombre a {Persona #A}
fijar B#nombre a {Persona #B}
fijar C#nombre a {Persona #C}

llamar EscribirLinea <A#padre>#nombre finargs

salir

instancia Arreglo arreglo
adquirir i
adquirir valor
instancia Referencia en

llamar arreglo#crear #(Final).

llamar arreglo#agregar Numero#Tipo #(Final).
llamar arreglo#agregar Numero#Tipo #(Final).
llamar arreglo#agregar Numero#Tipo #(Final).
llamar arreglo#agregar Numero#Tipo #(Final).

fijar i =* 0
fijar valor =* 1
llamar arreglo#obtener i #(Final).
recibir_resultado en#ref
fijar i =* valor
llamar en#fijarAttr i valor #(Final).

fijar i =* 1
fijar valor =* 5
llamar arreglo#obtener i #(Final).
recibir_resultado en#ref
fijar i =* valor
llamar en#fijarAttr i valor #(Final).

fijar i =* 2
fijar valor =* 4
llamar arreglo#obtener i #(Final).
recibir_resultado en#ref
fijar i =* valor
llamar en#fijarAttr i valor #(Final).

fijar i =* 3
fijar valor =* 7
llamar arreglo#obtener i #(Final).
recibir_resultado en#ref
fijar i =* valor
llamar en#fijarAttr i valor #(Final).

fijar i =* 0
llamar arreglo#obtener i #(Final).
recibir_resultado en#ref
fijar valor =* valor
llamar en#obtenerAttr valor #(Final).
recibir_resultado valor
escribir valor
nl

fijar i =* 1
llamar arreglo#obtener i #(Final).
recibir_resultado en#ref
fijar valor =* valor
llamar en#obtenerAttr valor #(Final).
recibir_resultado valor
escribir valor
nl

fijar i =* 2
llamar arreglo#obtener i #(Final).
recibir_resultado en#ref
fijar valor =* valor
llamar en#obtenerAttr valor #(Final).
recibir_resultado valor
escribir valor
nl

fijar i =* 3
llamar arreglo#obtener i #(Final).
recibir_resultado en#ref
fijar valor =* valor
llamar en#obtenerAttr valor #(Final).
recibir_resultado valor
escribir valor
nl

llamar arreglo#destruir #(Final).

llamar ImprimirFormato {Hola %\k} {Mundo} #(Final).

[
funcion Math
	llamar ImprimirFormato
		{El seno de % es %\k}
		{5}
		llamar Seno {5} finargs
	finargs
finfun

llamar Math finargs]

utilizar entsal.pseudo
utilizar cadena.pseudo

llamar EscribirLinea
    llamar Cadena#Concatenar
        {Hola }
        {Mundo}
    finargs
finargs

[ o también ]

adquirir mensaje
fijar mensaje a llamar Cadena#Buscar
    {hola mundo}
    {mun}
finargs
escribir mensaje
nl
liberar mensaje

[

adquirir valor
fijar valor a {Hola Mundo}

adquirir ref
fijar ref a valor#NOMBRE

fijar @ref a {Adios Mundo}
fijar ref a {mensaje}

escribir valor [ -> {Adios Mundo} ]
nl

[
<a>#a

a { b: ptr, c: attr }

a.new do A
a.new do B

a.b = &B;

<A#b>#c = (A.b).c
A#b#c = A.(b.c)

<a> = Referencia (ref: a)

adquirir yo
recibir_parametro yo
fijar <yo>#cadena a {Hola Mundo}

]

